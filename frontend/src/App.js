// Importing React library to use JSX and React functionality
import React from 'react';

// Functional component definition for the main App
function App() {
  return (
    // The outer div container that wraps the content
    <div>
      {/* Heading element that displays a message on the page */}
      <h1>Hello from React Frontend!</h1>
    </div>
  );
}

// Exporting the App component so it can be used in other parts of the application
export default App;
